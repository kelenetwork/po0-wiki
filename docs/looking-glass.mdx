---
pageType: custom
title: Looking Glass
---

import LookingGlass from '../src/components/LookingGlass';
import Po0Shell from '../src/components/Po0Shell';

<Po0Shell
  current="looking-glass"
  eyebrow="LOOKING GLASS"
  title={<>从在线节点<em>直接发起</em>。</>}
  lead="选择真实在线 agent，发起 Ping、TCPing、MTR、Nexttrace 或 Traceroute。Hub 只派发任务，不代跑。"
>
  <LookingGlass />
</Po0Shell>
